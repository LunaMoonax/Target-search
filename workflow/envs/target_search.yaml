name: target_search
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10          # Specify a compatible version for packages like pandas and primedrpa
  - parsnp
  - blast
  - pandas
  - primedrpa
